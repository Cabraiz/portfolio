import { DisplayUser } from "../models/DisplayUser.interface";
import { NewUser } from "../models/NewUser";

const register = (newUser: NewUser): Promise<DisplayUser | null> => {
    
}

const authSevice = {
   // register,
   // login,
   // lougout,
   // verifyJwt,
}

export default authSevice;